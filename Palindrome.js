
function palindrome(str) {
  var cleanStr = str.replace(/[^0-9a-z]/gi, '');
  var downCase = cleanStr.toLowerCase();
  var splitDownCase = downCase.split("");
    return true;
  }



palindrome("almostomla");